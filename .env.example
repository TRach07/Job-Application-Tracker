# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/jobtracker"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="" # Generate with: openssl rand -base64 32

# Google OAuth (for Gmail API access)
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# AI Provider
# Supported: groq, openai, mistral (all use OpenAI-compatible API)
AI_PROVIDER="groq"
AI_API_KEY="" # Get your key at the provider's dashboard
AI_MODEL=""   # Optional: override default model (e.g. gpt-4o, llama-3.3-70b-versatile)
